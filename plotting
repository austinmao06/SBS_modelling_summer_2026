t1 = 0
time_1 = np.argmin(np.abs(tau - t1))
plt.plot(zeta, b[time_1,:]**2, color="red", label=f"tau = {t1}")
plt.plot(zeta,f[time_1,:]**2, color="red", linestyle=":")
plt.plot(zeta, a[time_1,:], color="red", linestyle="--")
t2 = 2
time_2 = np.argmin(np.abs(tau - t2))
plt.plot(zeta, b[time_2,:]**2, color="green", label=f"tau = {t2}")
plt.plot(zeta,f[time_2,:]**2, color="green", linestyle=":")
plt.plot(zeta, a[time_2,:]**2, color="green", linestyle="--")
t3 = 10
time_3 = np.argmin(np.abs(tau - t3))
plt.plot(zeta, b[time_3,:]**2, color="blue", label=f"tau = {t3}")
plt.plot(zeta,f[time_3,:]**2, color="blue",linestyle=":")
plt.plot(zeta, a[time_3,:]**2, color="blue",linestyle="--")
#plt.xlim(left=-10, right = 2)
#plt.ylim(bottom = 0,top=3)
plt.title(f"Pulse Amplitude (Gaussian Profile) (g = {g}, gamms = {biggamma})")
plt.legend()
plt.grid()
plt.xlabel("ζ")
plt.ylabel(r"$b^2$")
#2_lorentz = (-4*zeta**3/(g*sigma**4)) * 1/(1+(zeta/sigma)**4)
#plt.plot(zeta,b2_lorentz, linestyle="--", color="black")
print(np.min(a[time_3,:]))
#plt.xlim(left = -5, right = 2)
